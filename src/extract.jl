"""
    netcdf2julia(str::String, var::String, poly::Vector)

Returns an array with the data in *str* of variable *var* inside the polygon *poly*
"""

function netcdf2julia(str::String, var::String, poly::Vector)


end
